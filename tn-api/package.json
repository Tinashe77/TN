{
  "name": "tn-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test *.test.js",
    "db:setup": "node --env-file-if-exists=.env database.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "mongodb": "^7.6.0",
    "nodemailer": "^10.0.10"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.3.0",
    "supertest": "^7.3.0"
  },
  "engines": {
    "node": ">=22.14.0 <23"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
