{
  "name": "tn-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "migrate": "node --env-file-if-exists=.env database.js",
    "test": "node --test *.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "nodemailer": "^10.0.10",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "supertest": "^7.3.0"
  },
  "engines": {
    "node": ">=22.14.0 <23"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
